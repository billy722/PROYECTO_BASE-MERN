# React Auth Starter Template

Proyecto base en React con:

- Autenticación JWT
- Auto logout por expiración de token
- Axios con interceptores
- Rutas protegidas (PrivateRoute + Outlet)
- Layout con Navbar
- CSS puro organizado
- Context + custom hook

## 🚀 Uso

```bash
git clone <repo>
npm install
npm run dev
